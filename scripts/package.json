{
  "name": "research-scripts",
  "version": "0.4.0",
  "description": "Runtime scripts for the research-structure workflow (bootstrap, prepare, summary, scaffolding).",
  "type": "module",
  "files": [
    "code/**/*.js",
    "docs/**/*.js",
    "lanes/**/*.js",
    "cli/**/*.js",
    "workflow/**/*.mjs",
    "workflow/*.mjs",
    "utils/*.mjs",
    "utils/*.js",
    "package.json"
  ],
  "ai-kod": {
    "type": "node",
    "required_tools": ["pnpm", "sqlite3"],
    "packages": []
  }
}
