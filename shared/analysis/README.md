# Shared analysis resources 📦

Эта папка — место для **shared** ресурсов между запусками (например, SQLite база в WAL режиме).

В репозитории мы **не храним** реальную БД (`*.db`, `*.db-wal`, `*.db-shm`) — это runtime‑артефакты.

