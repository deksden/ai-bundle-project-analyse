# Research Structure Demo

Этот примерный мини-проект предназначен для прогонов workflow `research-structure`.
Он содержит небольшой «монорепозиторий» с кодом, документацией и инфраструктурными файлами,
чтобы агент мог построить карту системы и выявить пробелы в покрытии.

## 📚 Состав репозитория

| Категория | Путь | Назначение |
| --- | --- | --- |
| Документация | `docs/ADR-001-current.md` | Актуальный ADR по API Gateway (совпадает с кодом). |
| Документация | `docs/ADR-999-legacy.md` | Устаревший ADR про платежи — код больше не существует. |
| Сервисы | `apps/api/src/index.ts` | HTTP API (основной подтверждённый сервис). |
| Сервисы | `apps/worker/src/index.ts` | Воркер, обрабатывающий очередь событий. |
| Сервисы | `apps/reporting/src/aggregator.ts` | Код без документации (должен подсветиться как code-only). |
| Инфраструктура | `services/cron/job.ts` | Cron-задача для очистки данных, без ADR. |
| Конфигурация | `configs/queue.yaml` | Описание очередей и топиков. |
| Workflow | `workflows/monitor.yaml` | Набросок workflow мониторинга. |
| Инфра | `infrastructure/terraform/main.tf` | Terraform-модуль с подключением PostgreSQL. |

## 📦 Данные для бандла

- `ingest.jsonl` — сущности, связи, очереди и внешние системы (используются в `clear-and-write-to-db.ts`).
- `remarks.json` — REMARKS, покрывающие как подтверждённые файлы, так и «сиротские» участки.
- `analysis/` — папка, в которую `research-structure` экспортирует JSON/Markdown отчёты.

## ✅ Что проверяет workflow

1. **Подтверждённый артефакт** — связка `apps/api` + `docs/ADR-001-current.md` + ingest evidence.
2. **Code-only** — новые сервисы (`apps/reporting`, `services/cron`), которые пока не описаны в документации.
3. **Doc-only / Legacy** — `docs/ADR-999-legacy.md`, на которую указывает REMARK.
4. **Внешние системы и очереди** — через metadata в `ingest.jsonl`.
5. **Remarks Coverage** — подсчёт применений и срез предупреждений (часть REMARK ограничена лимитом).

## 🚀 Быстрый старт

```bash
# 1. Убедитесь, что поднято окружение (clean-start + source .env.glm)
# 2. Запустите бандл через CLI
pnpm bundle:structure

# 3. Готовые артефакты появятся в
ls examples/research-structure-demo/analysis/
```

Полученные отчёты (`exports/*.json`, `reports/structure.md`, `global/structure/snapshot.json`)
используются в инструкции `.memory-bank/commands/wf-structure.md`.
