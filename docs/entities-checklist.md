# 📋 Checklist сущностей для research-structure

Документ описывает единый справочник сущностей, которые агент обязан искать во время стадий `code-process-file` и `docs-process-file`. Чеклист подключается к промптам напрямую (см. `bundle/scripts/workflow/*/process-file.mjs`), поэтому любые изменения в списке типов нужно вносить **только** сюда.

## 🔧 Формат отчёта

Каждый обработанный файл должен возвращать структуру:

```jsonc
{
  "file": {
    "relative_path": "apps/api/src/services/v7/event-orchestrator.ts",
    "absolute_path": "/repo/apps/api/src/services/v7/event-orchestrator.ts",
    "lane": "code|docs"
  },
  "summary": "1‑2 предложения о назначении файла",
  "checklist_results": [
    {
      "id": "component",
      "found": true,
      "count": 2,
      "entities": [
        {
          "name": "PriorityEventConsumer",
          "summary": "Класс, который читает приоритетные очереди Redis и передаёт payload диспетчеру",
          "source": { "lines": [15, 140] },
          "metadata": {
            "language": "ts",
            "exported": true,
            "dependencies": ["@ai-kod/redis"]
          }
        }
      ]
    }
  ],
  "deviations": []
}
```

- `checklist_results` должен содержать **все** типы из таблицы ниже (даже если ничего не найдено).  
- Для `found=false` заполняем `count: 0` и пустой массив `entities`.  
- `metadata` адаптируй к типу сущности (см. колонку “Что сохранить”).  
- В `deviations` фиксируй проблемы файла (например, “file_unparsable”, “missing_title”, “no_entities_detected”).

## ✅ Справочник сущностей

| ID | Где искать | Как распознать | Что сохранить |
| --- | --- | --- | --- |
| `system` | Документация | Описан целый продукт/подсистема уровня System (AI-KOD, Dashboard). | `name`, `scope`, ключевые обязанности, ссылки на другие контейнеры. |
| `domain_context` | Docs | Контекст/домен (Navigation, Observability). | `name`, `domain`, какие контейнеры входят, связанные события. |
| `container` | Оба | Самостоятельное приложение/сервис (API, Worker, CLI). В коде — корневой модуль с entrypoint. | `tech_stack`, `entry_file`, `interfaces`. |
| `component` | Оба | Модуль внутри контейнера (service, strategy, repository). | `layer` (Presentation/Application/Domain/Infrastructure), зависимости, экспортируемые функции. |
| `service` | Code | Класс/функция со стабильной обязанностью (TaskService, LeaseGuard). | `responsibility`, `patterns`, `depends_on`. |
| `repository` | Code | Слой доступа к данным (TaskRepository, RedisStorage). | `stores`, `operations`, `data_shapes`. |
| `handler` | Code | Обработчик событий/запросов (JobCompletedHandler, HTTP handler). | `handled_events`/`routes`, побочные эффекты. |
| `workflow` | Docs | Полный процесс/Workflow YAML. | `id`, `stages`, `inputs`, `quality_gates`. |
| `workflow_stage` | Оба | Конкретная стадия (prompt, fanout, validation). | `stage_id`, `kind`, `dependencies`. |
| `job_executor` | Code | Исполнитель задачи (StageJobOrchestrator, agent executor). | `provider`, `inputs`, `outputs`. |
| `api_endpoint` | Docs/Code | REST/GraphQL/SSE endpoint (`method + path`). | `method`, `path`, `auth`, `rate_limits`. |
| `cli_command` | Docs | CLI команда (ai-kod workflow run). | `command`, `arguments`, `examples`. |
| `event` | Оба | EventSystemAPI тип или доменное событие (`STEP_COMPLETED`). | `namespace`, `priority`, `payload`. |
| `queue_stream` | Code/Docs | Очередь, Topic, Redis Stream (Bull queue, `v7:events:*`). | `queue_key`, `priority`, `producer`/`consumer`. |
| `data_store` | Docs/Code | БД, KV, файловое хранилище. | `kind`, `technology`, `tables/keys`. |
| `external_system` | Docs | Внешний SaaS/SDK (Anthropic, Redis Cloud). | `name`, `purpose`, `integration_points`. |
| `config` | Code | Конфигурационные объекты, схемы ENV. | `env_vars`, `defaults`, `validation`. |
| `policy` | Docs | Правила, SLA, quality gates, security требования. | `policy_type`, `severity`, `conditions`. |
| `metric` | Docs/Code | Метрика/лог/health показатель. | `name`, `source`, `threshold`. |
| `test` | Code | Тестовый модуль, e2e сценарий. | `framework`, `cases`, `coverage`. |
| `language_class` | Code | Любой класс (включая utility). | `exported`, `extends`, `implements`. |
| `language_function` | Code | Функции/методы верхнего уровня. | `params`, `returns`, побочные эффекты. |
| `language_interface` | Code | TS interface/type alias. | `fields`, `usage`. |
| `language_constant` | Code | `const`/`enum`/`const object`. | `value_type`, `usage`. |
| `schema` | Оба | Schema (Zod, JSON Schema, YAML spec). | `schema_type`, `fields`, `validation_rules`. |
| `remark` | Docs | User/engineer guidance (remarks v1), инструкции. | `scope`, `impact`. |

> 🔁 При необходимости добавляй новые записи в таблицу — агенты автоматически начнут отрабатывать чеклист, если `bundle/scripts` подгрузят файл.

## 🧭 Рекомендации агенту

1. **Загрузи чеклист**: `const checklist = require("./../config/entities-checklist.json")` (или YAML через `js-yaml`).  
2. **Пройди по каждому типу**: определи, есть ли сущности данного вида в текущем файле.  
3. **Выдели сущности**: короткое имя, описание в 2–3 предложениях, координаты (строки или секция документа).  
4. **Заполни `checklist_results`**: даже если ничего не найдено, сохрани запись `found: false`.  
5. **Метрики**: для кодовых файлов обязательно подсчитай количество языковых сущностей (`language_class`, `language_function`, …). Для документов — количество разделов и описанных объектов.  
6. **Валидация**: если обязательные сущности (например, `component` для core-файлов) не найдены, отметь это в `deviations`.  
7. **Ссылки на исходник**: всегда указывай `source.relative_path` и или диапазон строк (`lines`) для кода, либо `section`/`heading` для документации.

## 🗂️ Расширения

- **Новый тип**: добавь строку в таблицу, синхронизируй с конфигом (`bundle/config/entities-checklist.yaml`) и обнови скрипты, если нужна кастомная обработка.  
- **Обязательные типы**: чтобы сделать тип обязательным для конкретного lane, добавь его ID в список `required_types` внутри соответствующего скрипта process-file.  
- **QA**: summary стадии должны агрегировать `count` по каждому ID и сверять с ожиданиями (например, “в кодовой линии должен быть хотя бы один `component` на каждые 500 строк кода”).

— Конец инструкции —
